#include <stdio.h>

int main(void) {
    int integer1, integer2, sum;
    printf("Enter first integer:\n");
    scanf("%d", &integer1);
    scanf("%d", &integer1);
    sum = integer1 + integer2;
    printf("Sum is %d\n", sum);
}